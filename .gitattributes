# 示例记忆库是定宽数据：LOG.txt 的第 N 条固定住在第 N×384 字节，热度账本同理。
# 任何行尾转换都会让记录多出一个字节、从此全盘错位，而读出来仍然像是数据。
#
# 每个库自己带的 .gitattributes 已经保住 LOG.txt 与 TREE/；HEAT/ 和会话流水目前
# 只是碰巧被 git 的二进制启发式认了出来（靠文件里有 NUL 字节），那是运气不是保证。
# 这一行把整棵示例库排除在行尾转换之外，不依赖任何猜测。
packs-sample/** -text
